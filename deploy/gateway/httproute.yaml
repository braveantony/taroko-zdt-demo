# 所有路徑都轉給 hydra Service:80(/version、/tour、/tour/events、/slow 全走這條)
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: hydra
spec:
  parentRefs:
    - name: hydra
  rules:
    - backendRefs:
        - name: hydra
          port: 80
